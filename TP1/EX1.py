# Cette fonction calcule la somme des éléments d'une liste donnée
def somme_liste(liste) :
    # Utilisation de la fonction intégrée `sum` pour calculer la somme des éléments de la liste
    return sum(liste)
liste = [1, 2, 3, 4]
print (somme_liste(liste))